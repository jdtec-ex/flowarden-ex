# Build artifacts
/target/

# Generated by Cargo
# Cargo.lock is often committed for applications, but we follow project convention
# (Existing Cargo.lock is present in the tree, so we don't ignore it here)

# Backup files
**/*.rs.bk

# Rust standard ignores
debug/
release/
*.pdb
